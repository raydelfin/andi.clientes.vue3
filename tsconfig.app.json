{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "include": ["env.d.ts", "src/**/*", "src/**/*.vue"],
  "exclude": ["src/**/__tests__/*"],
  "compilerOptions": {
    "incremental": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",

    /* -------------------------------------------------------- */
    /* OPCIONES AGREGADAS PARA DESACTIVAR LA ESTRICTEZ */
    "strictNullChecks": false, // Ignora errores de 'possibly null' (TS18047)
    "noImplicitAny": false,    // Permite tipos 'any' implícitos (más como JS)
    "skipLibCheck": true,      // Ignora errores de librerías como Ionic (string vs number)
    /* -------------------------------------------------------- */
    // 🚨 1. AGREGAR ESTA LÍNEA CRÍTICA
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
